import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query'; import {api} from '../lib/api'; import type {Incident} from '../types/api';
export const useIncident=(id?:string)=>useQuery({queryKey:['incident',id],queryFn:()=>api.get<Incident>(`/incidents/${id}/`).then(r=>r.data),enabled:Boolean(id)});
function post(path:string,payload:Record<string,unknown>){return api.post(path,payload).then(r=>r.data)}
export const useIncidentWorkflow=(id:number)=>{const client=useQueryClient();const refresh=()=>Promise.all([client.invalidateQueries({queryKey:['incident',String(id)]}),client.invalidateQueries({queryKey:['incidents']}),client.invalidateQueries({queryKey:['dashboard-summary']})]);const transition=useMutation({mutationFn:(payload:{status:string})=>post(`/incidents/${id}/status/`,payload),onSuccess:refresh});const troubleshoot=useMutation({mutationFn:(payload:Record<string,unknown>)=>post(`/incidents/${id}/troubleshooting/`,payload),onSuccess:refresh});const escalate=useMutation({mutationFn:(payload:Record<string,unknown>)=>post(`/incidents/${id}/escalate/`,payload),onSuccess:refresh});const resolve=useMutation({mutationFn:(payload:Record<string,unknown>)=>post(`/incidents/${id}/resolve/`,payload),onSuccess:refresh});const verify=useMutation({mutationFn:(payload:Record<string,unknown>)=>post(`/incidents/${id}/verify/`,payload),onSuccess:refresh});return {transition,troubleshoot,escalate,resolve,verify}};
